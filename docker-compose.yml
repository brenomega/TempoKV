services:
  tempokv:
    build:
      context: .
    image: tempokv:local
    environment:
      TEMPOKV_DATA_DIR: /data
      TEMPOKV_PERSISTENCE_ENABLED: "true"
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - tempokv-data:/data
    stop_grace_period: 15s

volumes:
  tempokv-data:
